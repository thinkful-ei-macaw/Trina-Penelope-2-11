'use strict';
const numList = [-2, 4 , 5, 783, -30, 43, 988];
function adverage(numbers){
    let Sum=0
    let i=0
    while (i< numbers.length) {
        Sum=Sum+ numbers[i]
        i++
         }
         return Sum/numbers.length
}

console.log (adverage(numList))
